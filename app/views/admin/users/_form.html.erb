<%= bootstrap_form_with(model: [:admin, user], local: true, label_errors: true) do |f| %>
  <%= f.text_field :email %>
  <%= f.text_field :password %>
  <%= f.check_box :admin, label: "Is an admin?  "%>

  <%= f.primary %>
<% end %>